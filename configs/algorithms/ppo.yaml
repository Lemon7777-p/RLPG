algorithm:
  name: ppo
  gamma: 0.99
  gae_lambda: 0.95
  learning_rate: 0.0003
  rollout_steps: 256
  minibatch_size: 64
  update_epochs: 4
  clip_coef: 0.2
  entropy_coef: 0.01
  value_loss_coef: 0.5
  max_grad_norm: 0.5
  target_kl: 0.02
